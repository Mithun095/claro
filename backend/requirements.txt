# Backend dependencies — Phase 1 skeleton.
# Feature deps (faster-whisper, ollama client, python-multipart, ...) are added
# in their own PLAN.md tasks, one slice at a time.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
